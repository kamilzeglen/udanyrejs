services:
    udanyrejs-frontend:
        build:
            context: ./web
            dockerfile: Dockerfile
        container_name: udanyrejs-frontend
        restart: always
        ports:
            - "3006:80"
        networks:
            - nginx-proxy-manager_default
            - udanyrejs_default

    udanyrejs-backend:
        build:
            context: ./api
            dockerfile: Dockerfile
        container_name: udanyrejs-backend
        env_file:
            - api/.env
        restart: always
        ports:
            - "5006:5006"
        volumes:
            - ./files/OFFERS_IMAGES:/api/files/OFFERS_IMAGES
            - ./files/SHIPS_IMAGES:/api/files/SHIPS_IMAGES
            - ./files/COMPANIES_IMAGES:/api/files/COMPANIES_IMAGES
            - ./files/OFFERS_PDFS:/api/files/OFFERS_PDFS
        networks:
            - nginx-proxy-manager_default
            - postgres_default
            - udanyrejs_default

    udanyrejs-scrapper:
        build:
            context: ./scrapper
            dockerfile: Dockerfile
        container_name: udanyrejs-scrapper
        env_file:
            - scrapper/.env
        restart: always
        ports:
            - "4006:4006"
        networks:
            - nginx-proxy-manager_default
            - udanyrejs_default

    udanyrejs-playwright:
        build:
            context: ./playwright
            dockerfile: Dockerfile
        container_name: udanyrejs-playwright
        restart: always
        ports:
            - "6006:6006"
        networks:
            - udanyrejs_default

networks:
    nginx-proxy-manager_default:
        external: true
    postgres_default:
        external: true
    udanyrejs_default:
        external: true
